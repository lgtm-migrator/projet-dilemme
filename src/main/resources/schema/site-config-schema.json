{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Site configuration",
  "description": "Site configuration",
  "required": [ "title", "owner", "domain"],
  "type": "object",
  "properties": {
    "title": {
      "description": "Web site's title",
      "type": "string"
    },
    "owner": {
      "description": "Web site's owner",
      "type": "string"
    },
    "domain": {
      "type": "string",
      "description" : "web site's FQDN"
    }
  }
}